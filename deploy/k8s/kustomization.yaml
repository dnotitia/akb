apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: akb

resources:
  - base

# Compatibility entry point. New installations select an explicit directory
# under profiles/, all of which compose the same canonical base.
